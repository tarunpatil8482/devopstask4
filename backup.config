# backup.config - edit these values as needed

# Where backups will be stored (use ./backups to keep them inside your project)
BACKUP_DESTINATION="./backups"

# Folders or file patterns to exclude (comma separated)
EXCLUDE_PATTERNS=".git,node_modules,.cache"

# Rotation policy
DAILY_KEEP=7

# Lockfile path
LOCKFILE="/tmp/backup.lock"

# Logfile path (under BACKUP_DESTINATION)
LOGFILE="$BACKUP_DESTINATION/backup.log"

# Hash command (sha256sum or md5sum)
HASH_CMD="sha256sum"
# backup.config - edit these values as needed

# Where backups will be stored (use ./backups to keep them inside your project)
BACKUP_DESTINATION="./backups"

# Folders or file patterns to exclude (comma separated)
EXCLUDE_PATTERNS=".git,node_modules,.cache"

# Rotation policy
DAILY_KEEP=7

# Lockfile path
LOCKFILE="/tmp/backup.lock"

# Logfile path (under BACKUP_DESTINATION)
LOGFILE="$BACKUP_DESTINATION/backup.log"

# Hash command (sha256sum or md5sum)
HASH_CMD="sha256sum"
